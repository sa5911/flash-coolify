# ==========================================
# DOKPLOY ENVIRONMENT VARIABLES
# Copy and paste all of this into your Dokploy Project -> Environment
# ==========================================

# 1. DATABASE CONNECTION
# (Password has been URL-encoded to handle special characters)
DATABASE_URL=postgresql://flash-erp-user:Flashtechdeploy%400333dokpoly%40@46.202.194.55:5432/flash-erp-db

# 2. SECURITY (BACKEND)
# This key is used for session security. Verify this or generate a new one.
SECRET_KEY=b9c4f7a1d8e2b5c8a3f6e9d2c5b8a1f4d7e0c3b6a9d2c5b8a1f4d7e0c3b6a9d2
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 3. BACKEND CONFIGURATION
# The backend runs on port 8000 internally.
APP_NAME=Flash ERP
APP_VERSION=1.0.0
DEBUG=false
# This must match the Internal/Local URL where the frontend can reach it.
# Since they are in the same container, localhost:8000 is correct for server-side calls.
# For Client-side CORS (if needed), add your Production Domain below (e.g. https://flash-erp.com).
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# 4. FRONTEND CONFIGURATION
# This tells the frontend code to use relative paths for API calls (proxied by Next.js)
NEXT_PUBLIC_API_BASE_URL=/api
# This is used by next.config.js to know where to proxy requests to
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# 5. GENERAL
NODE_ENV=production
UPLOADS_DIR=/app/uploads

# NOTE: Railpack will provide the $PORT variable automatically.
