from setuptools import setup, find_packages

setup(
    name="flash-erp-backend",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[
        # Dependencies are in requirements.txt
    ],
    python_requires=">=3.11",
)
